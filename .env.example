# Database Configuration
# Choose ONE of the following options:

# Option 1: Direct Database URL (recommended for Neon)
DATABASE_URL="postgresql://username:password@ep-example-123456.us-east-1.aws.neon.tech/neondb?sslmode=require"

# Option 2: Neon Database with separate API key (alternative)
# NEON_DATABASE_URL="your-database-name"
# NEON_API_KEY="your-api-key"

# Database connection will use the first available option
