# 家庭开销记录器

一个简单易用的家庭开支管理应用，使用 Next.js 和现代前端技术栈构建。

## ✨ 功能特性

- ✅ 添加、编辑、删除开支记录
- 📊 开支统计和可视化
- 🏷️ 预定义和自定义分类
- 💾 本地数据存储（演示模式）
- 📱 响应式设计

## 🛠️ 技术栈

- **框架**: Next.js 15 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **UI组件**: Radix UI + shadcn/ui
- **状态管理**: Zustand
- **图标**: Lucide React
- **验证**: Zod
- **表单**: React Hook Form

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 运行开发服务器

```bash
npm run dev
```

在浏览器中打开 [http://localhost:3000](http://localhost:3000) 查看结果。

### 构建生产版本

```bash
npm run build
npm run start
```

## 📁 项目结构

```
/
├── app/                    # Next.js App Router 页面
│   ├── globals.css         # 全局样式
│   ├── layout.tsx         # 根布局
│   └── page.tsx           # 主页
├── components/             # React 组件
│   └── ui/                 # 基础 UI 组件
├── lib/                    # 工具函数
├── services/              # 业务逻辑服务
├── store/                  # Zustand 状态管理
├── types/                  # TypeScript 类型定义
├── constants/              # 常量定义
├── .42cog/                 # 42COG 框架文档
│   ├── real.md            # 现实约束
│   ├── cog.md             # 认知模型
│   ├── spec-*.md          # 规范文档
└── spec/                   # 设计文档
```

## 🎯 核心概念

### 42COG 认知敏捷法

本项目采用 42COG (Cognitive Agility) 方法论，包含以下核心概念：

- **RCSW 工作流**: Real → Cog → Spec → Work
- **可供性理论**: 关注行动可能性而非功能列表
- **MAS (最小可供性故事)**: 替代传统 MVP 的故事驱动方法

### 三种最小故事

- **Light Story**: 坏到好的转变
- **Dark Story**: 好到坏的挑战
- **Grey Story**: 循环的日常操作

## 📊 数据模型

### 开支记录 (Expense)

```typescript
{
  id: string;
  amount: number;           // 开销金额
  categoryId: string;      // 分类ID
  description: string;      // 描述
  date: Date;               // 日期
  createdAt: Date;
  updatedAt: Date;
}
```

### 分类 (Category)

```typescript
{
  id: string;
  name: string;            // 分类名称
  slug: string;             // URL 友好的标识
  color: string;            // 十六进制颜色值
  isPredefined: boolean;    // 是否为预定义分类
}
```

## 🎨 设计系统

### 色彩方案

- **主色调**: 蓝绿色 (180°)
- **辅助色**: 绿色 (150°)
- **中性色**: 灰色系列

### 响应式断点

- 移动端: < 640px
- 平板: 640px - 1024px
- 桌面端: > 1024px

## 🔧 开发指南

### 状态管理

使用 Zustand 进行状态管理，支持本地持久化：

```typescript
const { expenses, addExpense } = useExpenseStore();
```

### 添加新功能

1. 在 `types/` 中定义 TypeScript 类型
2. 在 `store/` 中创建 Zustand store
3. 在 `components/` 中创建 React 组件
4. 在 `app/` 中创建页面

### 代码规范

- 使用 TypeScript 严格模式
- 遵循 ESLint 规则
- 使用 Prettier 格式化
- 组件使用 PascalCase
- 文件和函数使用 camelCase

## 📝 待办功能

- [ ] 用户认证系统
- [ ] 数据库集成
- [ ] 图表可视化
- [ ] 数据导出
- [ ] 移动端优化
- [ ] 暗色模式

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT

## 🙏 致谢

- [Next.js](https://nextjs.org/)
- [Tailwind CSS](https://tailwindcss.com/)
- [Radix UI](https://www.radix-ui.com/)
- [shadcn/ui](https://ui.shadcn.com/)
- [Zustand](https://github.com/pmndrs/zustand)
